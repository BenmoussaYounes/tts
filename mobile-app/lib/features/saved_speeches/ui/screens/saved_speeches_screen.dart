import 'package:flutter/material.dart';

class SavedSpeechesScreen extends StatelessWidget {
  const SavedSpeechesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Saved Speeches Screen'));
  }
}
